@model Calligraphy.Models.ContentBoxModel

<div class="@Model.CssClasses">
    <div class="home-container-heading">
        <h2>@Model.Title</h2>
    </div>
    <div class="home-container-bottom">@Html.Raw(@Model.Content)</div>
</div>